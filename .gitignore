*.beam
*.deb
.combo_dialyzer_plt
.DS_Store
.eqc-info
.eunit
.qc/
.rebar/
.rebar3/
.tool-versions
.vagrant
/.vscode
/_notes
/config/_ssl
/doc
/erlang_ls.config
/rebar3.crashdump
_build/
_checkouts.old/
_checkouts/
# Log of a heavy-suite run, written by test/fly/run.sh.
/fly-*.log
# Cache of the OTP test suites, fetched per OTP version by
# test/fetch_otp_test_sources.sh. An OTP release does not ship these suites, and
# they are fetched rather than committed so that a new OTP version needs no new
# copy here.
/otp_src/
build
current_counterexample.eqc
data-root*/
data/
deps.test/
deps/*
dev
dialyzer_warnings
distdir
ebin/*
erl_crash.dump
fprof.trace
log/
Mnesia*
node*/
package
priv/logs/*.csv
priv/plots/*.pdf
results.csv
riak_test/ebin/*.beam
rt/
src/*.swp
tags
TEST-*
test/*/RAND

# Fly.io private config (account/org/deploy token) — never commit.
# The tracked template is test/fly/.env.example.
.env
test/fly/.env